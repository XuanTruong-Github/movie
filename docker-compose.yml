version: "3.8"

services:
  movie:
    build:
      context: .
      dockerfile: Dockerfile
    image: movie-app:latest
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    ports:
      - "80:3000"
